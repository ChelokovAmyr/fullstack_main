# Интернет-магазин - Fullstack проект

Полноценный интернет-магазин senior уровня с Next.js (frontend) и NestJS (backend).

## 🚀 Технологии

### Frontend
- **Next.js 14** с TypeScript и App Router
- **React Query** для управления серверным состоянием
- **Axios** для HTTP запросов
- **React Query Devtools** для отладки
- Адаптивный дизайн с современным UI

### Backend
- **NestJS** с TypeScript
- **TypeORM** для работы с базой данных
- **PostgreSQL** как основная БД
- **JWT** для аутентификации
- **Passport** для стратегий аутентификации
- **bcrypt** для хеширования паролей
- **Swagger** для документации API
- **Helmet** для безопасности
- **Throttler** для rate limiting

## ✨ Функционал

### Пользовательский функционал
- ✅ Регистрация и авторизация
- ✅ Каталог товаров с поиском, фильтрами и сортировкой
- ✅ Корзина покупок (сохранение в БД для авторизованных)
- ✅ Оформление заказов с адресом доставки
- ✅ Личный кабинет с историей заказов
- ✅ Избранное (wishlist)
- ✅ Отзывы и рейтинги товаров
- ✅ Пагинация товаров

### Админ панель
- ✅ Управление товарами (CRUD)
- ✅ Управление категориями
- ✅ Управление заказами (изменение статусов)
- ✅ Роли пользователей (Admin/Customer)

### Безопасность
- ✅ JWT токены для аутентификации
- ✅ Rate limiting (100 запросов/минуту)
- ✅ Helmet для HTTP заголовков безопасности
- ✅ Валидация данных на backend
- ✅ Защита роутов с guards

## 📋 Требования

- Node.js 18+
- PostgreSQL 15+ (или Docker)
- npm или yarn

## 🛠️ Установка

1. Установите зависимости для всех проектов:
```bash
npm run install:all
```

2. Настройте переменные окружения:

### Backend
Создайте файл `backend/.env`:
```env
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=1234
DB_DATABASE=my_fullstack_db
JWT_SECRET=your-secret-key-change-in-production
PORT=3001
NODE_ENV=development
```

### Frontend
Создайте файл `frontend/.env.local`:
```env
NEXT_PUBLIC_API_URL=http://localhost:3001
```

## 🐳 Запуск PostgreSQL с Docker

Если у вас не установлен PostgreSQL локально:

```bash
docker-compose up -d
```

Это создаст контейнер PostgreSQL с настройками:
- Хост: localhost
- Порт: 5432
- Пользователь: postgres
- Пароль: 1234
- База данных: my_fullstack_db

## 🚀 Запуск проекта

### Параллельный запуск (рекомендуется)

```bash
npm run dev
```

Backend: http://localhost:3001
Frontend: http://localhost:3000
Swagger API: http://localhost:3001/api

### Отдельный запуск

#### Backend
```bash
npm run dev:backend
```

#### Frontend
```bash
npm run dev:frontend
```

## 📦 Заполнение базы данных

После запуска backend, выполните seed скрипт для заполнения базы товарами:

```bash
cd backend
npm run seed
```

Это создаст:
- Админ пользователя (email: admin@shop.com, password: admin123)
- 6 категорий товаров
- 16 товаров в разных категориях

## 📚 API Endpoints

### Аутентификация
- `POST /auth/register` - Регистрация
- `POST /auth/login` - Вход

### Товары
- `GET /products` - Список товаров (с фильтрами, поиском, сортировкой)
- `GET /products/:id` - Детали товара
- `POST /products` - Создать товар (Admin)
- `PATCH /products/:id` - Обновить товар (Admin)
- `DELETE /products/:id` - Удалить товар (Admin)

### Категории
- `GET /categories` - Список категорий
- `GET /categories/:id` - Детали категории
- `POST /categories` - Создать категорию (Admin)

### Корзина
- `GET /cart` - Получить корзину
- `POST /cart` - Добавить в корзину
- `PATCH /cart/:id` - Изменить количество
- `DELETE /cart/:id` - Удалить из корзины

### Заказы
- `GET /orders` - Список заказов
- `POST /orders` - Создать заказ
- `PATCH /orders/:id` - Обновить статус (Admin)

### Отзывы
- `GET /reviews?productId=...` - Отзывы товара
- `POST /reviews` - Создать отзыв

### Избранное
- `GET /wishlist` - Список избранного
- `POST /wishlist/:productId` - Добавить в избранное
- `DELETE /wishlist/:productId` - Удалить из избранного

Полная документация API доступна в Swagger: http://localhost:3001/api

## 🏗️ Структура проекта

```
.
├── backend/
│   ├── src/
│   │   ├── auth/          # Аутентификация
│   │   ├── users/         # Пользователи
│   │   ├── products/      # Товары
│   │   ├── categories/    # Категории
│   │   ├── orders/        # Заказы
│   │   ├── cart/          # Корзина
│   │   ├── reviews/       # Отзывы
│   │   ├── wishlist/      # Избранное
│   │   ├── database/      # Seed скрипты
│   │   └── main.ts
│   └── package.json
├── frontend/
│   ├── src/
│   │   ├── app/           # Next.js страницы
│   │   │   ├── products/  # Каталог и карточки товаров
│   │   │   ├── cart/      # Корзина
│   │   │   ├── checkout/  # Оформление заказа
│   │   │   └── profile/   # Личный кабинет
│   │   ├── components/    # React компоненты
│   │   ├── lib/           # API клиент и утилиты
│   │   └── providers/     # React провайдеры
│   └── package.json
├── docker-compose.yml
└── package.json
```

## 🧪 Разработка

### Backend
- `npm run start:dev` - Запуск с hot reload
- `npm run build` - Сборка
- `npm run seed` - Заполнить базу данными
- `npm run migration:generate` - Создать миграцию
- `npm run migration:run` - Применить миграции

### Frontend
- `npm run dev` - Запуск в режиме разработки
- `npm run build` - Сборка
- `npm run start` - Production режим

## 🔒 Безопасность

- Rate limiting: 100 запросов в минуту
- Helmet для защиты от XSS и других атак
- Валидация всех входных данных
- JWT токены с истечением через 24 часа
- Хеширование паролей с bcrypt (10 раундов)

## 📝 Миграции базы данных

В режиме разработки TypeORM автоматически синхронизирует схему. Для production:

```bash
cd backend
npm run migration:generate -- -n MigrationName
npm run migration:run
```

## 🎯 Особенности

- **Поиск и фильтры**: По названию, категории, цене, рейтингу
- **Сортировка**: По цене, рейтингу, дате, названию
- **Пагинация**: Для больших каталогов
- **Адаптивный дизайн**: Работает на всех устройствах
- **Lazy loading**: Изображения загружаются по требованию
- **Транзакции**: Безопасное создание заказов
- **Автоматический расчет рейтинга**: На основе отзывов

## 📄 Лицензия

MIT
